// Code generated by goa, DO NOT EDIT.
//
// tags service
//
// Command:
// $ goa gen github.com/e601201/life-api/design

package tags

import (
	"context"

	goa "goa.design/goa/v3/pkg"
	"goa.design/goa/v3/security"
)

// Tags of the authenticated user
type Service interface {
	// List the authenticated user's tags with entry counts, sorted by name
	List(context.Context, *ListPayload) (res []*Tag, err error)
	// Rename a tag (applies to every entry carrying it)
	Update(context.Context, *UpdatePayload) (res *Tag, err error)
	// Delete a tag and detach it from every entry
	Delete(context.Context, *DeletePayload) (err error)
}

// Auther defines the authorization functions to be implemented by the service.
type Auther interface {
	// JWTAuth implements the authorization logic for the JWT security scheme.
	JWTAuth(ctx context.Context, token string, schema *security.JWTScheme) (context.Context, error)
}

// APIName is the name of the API as defined in the design.
const APIName = "life"

// APIVersion is the version of the API as defined in the design.
const APIVersion = "0.0.1"

// ServiceName is the name of the service as defined in the design. This is the
// same value that is set in the endpoint request contexts under the ServiceKey
// key.
const ServiceName = "tags"

// MethodNames lists the service method names as defined in the design. These
// are the same values that are set in the endpoint request contexts under the
// MethodKey key.
var MethodNames = [3]string{"list", "update", "delete"}

// DeletePayload is the payload type of the tags service delete method.
type DeletePayload struct {
	// Tag ID
	ID int64
	// JWT (Authorization: Bearer <token>)
	Token *string
}

// ListPayload is the payload type of the tags service list method.
type ListPayload struct {
	// JWT (Authorization: Bearer <token>)
	Token *string
}

// Tag is the result type of the tags service update method.
type Tag struct {
	ID   int64
	Name string
	// このタグが付いている記録の数
	EntryCount int
}

// UpdatePayload is the payload type of the tags service update method.
type UpdatePayload struct {
	// Tag ID
	ID int64
	// 新しい名前
	Name string
	// JWT (Authorization: Bearer <token>)
	Token *string
}

// MakeUnauthorized builds a goa.ServiceError from an error.
func MakeUnauthorized(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "unauthorized", false, false, false)
}

// MakeNotFound builds a goa.ServiceError from an error.
func MakeNotFound(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "not_found", false, false, false)
}

// MakeConflict builds a goa.ServiceError from an error.
func MakeConflict(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "conflict", false, false, false)
}
